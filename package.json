{
  "name": "playwright-browser-agent",
  "version": "1.0.0",
  "description": "Browser automation service using Playwright",
  "scripts": {
    "install": "pip install -r requirements.txt && playwright install chromium",
    "dev": "uvicorn main:app --reload",
    "start": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "build": "echo 'Python project - no build step required'"
  },
  "keywords": ["playwright", "browser", "automation", "fastapi"],
  "author": "",
  "license": "MIT"
}